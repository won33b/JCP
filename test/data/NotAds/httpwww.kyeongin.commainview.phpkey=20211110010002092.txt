     <meta http-equiv="Cache-Control" content="no-cache">
            <meta http-equiv="Pragma" content="no-cache">
            <meta http-equiv="Expires" content="-1">
        
			var wheight = $(window).height();
			$(".ico-search").click(function(){
				//$(".srh-dim").show();
				$(".srh-dim").css('display','block');
				$("body,html").css("overflow-y","hidden");
				$(".allmenu").css("height",wheight);
				$(".srh-dim").css("height",wheight);
			});
			$(".srh-area .srh-close").click(function(){
				$(".srh-dim").hide();
				$("body,html").css("overflow-y","auto");
			});
		})
        var agent = navigator.userAgent.toLowerCase();
        var cookiedata = document.cookie;
		


        if (cookiedata.indexOf('browser_update_cookie=done') < 0) {
            var canvas = document.createElement('canvas');
            if ((agent.indexOf('msie 7') > -1) && (agent.indexOf('trident') > -1 )) {
                drawModal(true);
            } else if (agent.indexOf('rv:11') > -1) {
                drawModal(true);
            } else if (!('getContext' in canvas)) {
                drawModal(true);
            } else if (agent.indexOf('msie') != -1) {
                drawModal(false);
            }
        }

        function closePopup(t) {
            if (t == 24) {
                setCookie('browser_update_cookie', 'done', 24);
                document.getElementById('browser_update').style.display = 'none';
                document.body.style.paddingTop = '0px';
            } else {
                document.getElementById('browser_update').style.display = 'none';
                document.body.style.paddingTop = '0px';
            }
        }

        function setCookie(name, value, expirehours) {
            var todayDate = new Date();
            todayDate.setHours(todayDate.getHours() + expirehours);
            document.cookie = name + '=' + escape(value) + '; path=/; expires=' + todayDate.toGMTString() + ';';
        }

        function drawModal(isCompatible) {
            /
            if (isCompatible) {
                document.write(modal);
            } else {
                document.addEventListener('DOMContentLoaded', function() {
                    var modalWrap = document.createElement('div');
                    modalWrap.innerHTML = modal;
                    document.body.appendChild(modalWrap);

	<div class="head-top sticky-top">
		<div class="hd-qmenu">
			<button class="ico-img btn-allmenu ico-allmenu"><span class="blind">전체메뉴</span></button>
			<button class="ico-img btn-search ico-search"><span class="blind">검색</span></button>
		</div>
		<h2 class="small-logo"><a href="http://www.kyeongin.com/main/"></a></h2>
		<nav class="gnb">
              			$(document).mouseup(function (e){
				var layer_pop = $(".allmenu");
				if(layer_pop.has(e.target).length === 0){
					layer_pop.css("display","none");
	
	</script>
	<div class="allmenu" style="height: 969px;">
        <div class="allmenu-area">
            <div class="allmenu-top">
                <p class="kiib-logo"><a href="http://www.kyeongin.com/main/"></a></p>
                <span class="ico-img ico-close"></span>
            </div>

            <div class="allmenuarea" style="height: 859px;">
                <div class="allmenu-box">
                    <ul class="depth01">
                        <li>
                            <a href="http://www.kyeongin.com/main/">뉴스홈</a>
                            <div class="depth02" style="display: none; height: 969px;">
.srh-bott{width:100%; margin-top:40px; clear:both;}
.srh-keyword{width:420px; float:left;}
.srh-keyword h3{font-size:24px; color:#222; font-weight:500; margin-bottom:25px;}
.srh-keyword ul{overflow:hidden;}
.srh-keyword > ul li{float:left; background:#f1f5f8; border-radius:30px; font-size:16px; margin-right:20px; margin-bottom:10px;}
.srh-keyword > ul li a{color:#222; display:block; padding:10px 20px;}
.m-pop-keyword{margin:15px 20px 10px 20px; width:calc(100% - 40px); overflow:hidden;}
.m-pop-keyword h3{font-size:1.25em; margin:10px 0;}
.m-pop-keyword ul li{float:left; border-radius:30px; background:#f1f5f8; padding:5px 15px; margin-right:10px; margin-bottom:10px;}
</script><script type="text/javascript">
    <!--
    var fontSize = 18;
    var lineHeight = 31;

    function fontPlus() {
        if (fontSize < 23) {
            fontSize = fontSize + 2;
            setFontSize();
        }
    }
    function fontMinus() {
        if (fontSize >= 13) {
            fontSize = fontSize - 2;
            if (fontSize < 12) {
                fontSize = 12;
            setFontSize();
    function setFontSize() {
        lineHeight = fontSize+Math.round(2*fontSize);
        obj = document.getElementById("font");
        obj.style.fontSize = fontSize+"px";
    $(function(){
        var stopCode = "N05";
        if( stopCode != "N13" ){
            $("#inTextAd01").show();
            var inbody = $(".view_left .view_txt");
            var intextAd = $("#inTextAd01");
            var searchArea = inbody;
            var nodeArray =[];
            var lastNode= "", totalLength = 0, smallLength = 0, smallLength2 = 0, findLength = 0, baseLength = 6;
            inbody.contents().each(function(){if( $(this).hasClass("news_content") ){searchArea = $(this);}});
            totalLength = searchArea.text().length;
    var fontSize = 18;
    var lineHeight = 31;

    function fontPlus() {
        if (fontSize < 23) {
            fontSize = fontSize + 2;
            setFontSize();
    function fontMinus() {
        if (fontSize >= 13) {
            fontSize = fontSize - 2;
            if (fontSize < 12) {
                fontSize = 12;
            setFontSize();
    function setFontSize() {
        lineHeight = fontSize+Math.round(2*fontSize);
        obj = document.getElementById("font");
        obj.style.fontSize = fontSize+"px";
